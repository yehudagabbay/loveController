# בדיקות לפני פרסום - LIBA

מטרת הקובץ: רשימת בדיקות ידניות לפני פרסום האפליקציה והשרת.

העדיפות בביצוע:
1. בדיקות קריטיות לחסימת פרסום
2. בדיקות מסך-מסך
3. בדיקות שפה, קישורים ושיתוף
4. בדיקות שרת לאחר פריסה

## 1. בדיקות חסימת פרסום

- רישום משתמש רגיל עובד מקצה לקצה
- התחברות עם משתמש רגיל עובדת
- מייל אימות נשלח ונפתח
- לחיצה על קישור איפוס סיסמה פותחת את מסך `NewPassword`
- איפוס סיסמה עובד בפועל וניתן להתחבר עם הסיסמה החדשה
- שליפת קלפים עובדת בכל מצבי המשחק: זוגות, חברים/עבודה, משפחה
- אין קריסת שרת `500.30`
- אין מסך אדום של React Native
- אין טקסטים שבורים, ג'יבריש או `??` במסכים המרכזיים

## 2. הכנה לבדיקה

- לבדוק על Android אמיתי
- לבדוק עם חיבור אינטרנט תקין
- לבדוק לפחות עם שני משתמשים:
  - משתמש רגיל עם מייל וסיסמה
  - משתמש Social אם קיים
- לוודא שעל השרת קיימים:
  - `appsettings.Secrets.json`
  - `.secrets/service-account.json`

## 3. מסכי כניסה והרשמה

### Welcome

- פתיחת האפליקציה מציגה את `Welcome`
- כפתור `Login` מוביל למסך התחברות
- כפתור `Register` מוביל למסך הרשמה
- מחליף שפה עובד והמסך מתרענן

### Registration

- כל שדות החובה מוצגים
- ולידציית מייל לא תקין עובדת
- ולידציית גיל מתחת ל-18 עובדת
- רישום משתמש חדש מצליח
- ברישום עם מייל שכבר קיים מתקבלת הודעה נכונה
- לאחר רישום מתקבל מייל אימות

### Login

- התחברות עם פרטים נכונים מצליחה
- התחברות עם סיסמה שגויה מציגה הודעה תקינה
- התחברות עם משתמש לא מאומת מציעה שליחה מחדש של מייל אימות
- כפתור `Forgot password` או פתיחת מודאל איפוס סיסמה עובדים

### SocialRegister

- התחברות Google נפתחת
- חזרה מה-flow של Google פותחת את האפליקציה
- אם חסרים פרטי משתמש, המשתמש מועבר ל-`UpdateDetails`
- social login קיים לא נשבר אחרי שינויי ה-deep link

### ResetPassword / NewPassword

- בקשת איפוס סיסמה מחזירה תשובה תקינה
- מתקבל מייל איפוס
- לחיצה על קישור מהמייל פותחת את האפליקציה
- המשתמש מועבר למסך `NewPassword`
- `email` ו-`token` מגיעים למסך
- שינוי סיסמה מצליח
- לאחר שינוי סיסמה ניתן להתחבר עם הסיסמה החדשה
- שימוש חוזר באותו token נכשל

## 4. GameModeSelect

- המסך נפתח לאחר התחברות
- כרטיס זוגות מוביל ל-`GameHome`
- כרטיס משפחה מוביל ל-`FamilyCardsSelect`
- כרטיס עבודה/חברים מוביל ל-`FriendsCardsSelect`
- מצב `friends_fun` מציג הודעת `coming soon`
- בכניסה לזוגות מופיע `AgeGate` למשתמש שלא אישר 18+
- לאחר אישור 18+ ניתן להיכנס שוב בלי מודאל אם נשמר

## 5. TopMenu

- כפתור התפריט נפתח בכל המסכים המרכזיים
- ניווט מתוך התפריט עובד לכל היעדים:
  - `GameModeSelect`
  - זוגות
  - משפחה
  - חברים/עבודה
  - `UpdateDetails`
  - `FeedbackScreen`
  - `Help`
  - `About`
  - `PrivacyPolicy`
- החלפת שפה מתוך התפריט עובדת
- `Terms of Use` ו-`Privacy Policy` נפתחים
- Logout עובד ומחזיר ל-`Welcome`
- אם `Reset instructions` עדיין קיים, לוודא שהוא מאפס את כל מודאלי ההוראות

## 6. UpdateDetails

- המסך נפתח מתוך התפריט
- ניתן לעדכן שם, מין וגיל
- שמירה מצליחה
- הודעת הצלחה מוצגת
- הנתונים נשמרים בשרת
- אין טקסטים באנגלית בשפות שתוקנו

## 7. זוגות - CoupleCardsSelect

- מודאל הוראות זוגות מופיע למשתמש חדש
- סימון `Don't show again` עובד רק למשתמש הנוכחי
- ההוראות לא קופצות במסכים אחרים
- הזנת שני שמות שחקנים עובדת
- בחירת רמות בכרטיסיות `intro`, `fun`, `passion` עובדת
- `Select all` ו-`Clear` עובדים
- Special draw:
  - favorite
  - feedback
  - shared
- התחלת משחק בלי בחירת קלפים מציגה אזהרה תקינה
- אם אין קלפים תואמים מתקבלת הודעה תקינה
- כפתור התחלה עובד ומעביר ל-`IndexGame`

## 8. זוגות - CoupleCardsGame

- שליפת קלף פותחת מודאל קלף
- `We did it` מסיים קלף ומעביר תור
- `Skip` מדלג ומעביר תור
- `Share` פותח `ShareCardModal`
- `Feedback` פותח `FeedbackModal`
- כפתורי `Liked` ו-`Loved` עובדים
- סטטוס המשחק מתעדכן
- הקלף האחרון מוצג
- אין חפיפה בין הכפתורים ל-safe area
- אין חיתוך טקסט בכפתורי הפעולה

## 9. חברים/עבודה - FriendsCardsSelect

- מודאל הוראות חברים/עבודה מופיע למשתמש חדש
- `Don't show again` עובד פר משתמש
- ניהול שחקנים דינמי עובד:
  - הוספת שחקן
  - מחיקת שחקן
  - placeholders תקינים
- בחירת קטגוריות `intro`, `fun`, `team` עובדת
- special draw עובד
- התחלת משחק מובילה ל-`FriendsCardsGame`

## 10. חברים/עבודה - FriendsCardsGame

- אם אין שחקנים, מוצג מסך fallback וחזרה למסך הבחירה
- שליפת קלף עובדת
- `ShareCardModal` נפתח
- `Feedback` נשלח בהצלחה
- `Liked` ו-`Loved` עובדים
- סטטוס המשחק מתעדכן
- מעבר בין שחקנים תקין

## 11. משפחה - FamilyCardsSelect

- מודאל הוראות משפחה מופיע למשתמש חדש
- `Don't show again` עובד פר משתמש
- ניהול שחקנים דינמי עובד
- אפשר להתחיל עם 2 שחקנים ומעלה
- בחירת רמות וקטגוריות עובדת
- special draw עובד
- התחלת משחק מובילה ל-`FamilyCardsGame`

## 12. משפחה - FamilyCardsGame

- אם אין שחקנים, מוצג מסך fallback
- שליפת קלף עובדת
- `ShareCardModal` נפתח
- שליחת משוב עובדת
- `Liked` ו-`Loved` עובדים
- התראה על חבילת קלפים ריקה מוצגת
- סטטוס התקדמות ותור שחקן מתעדכנים

## 13. ShareCard / ShareCardModal

- פתיחת שיתוף מכל מצבי המשחק עובדת
- יצירת תמונת שיתוף מצליחה
- אין `??` בתמונת השיתוף
- טקסט תחתון בתמונה לא נחתך
- סוג המשחק מתורגם נכון:
  - זוגות
  - חברים
  - משפחה
- כפתור השיתוף עצמו מתורגם

## 14. FeedbackScreen

- פתיחה מתוך התפריט עובדת
- שליחת feedback דרך המייל/קישור עובדת
- הכתובת `liba.supp@gmail.com` מוצגת נכון
- כל מפתחות התרגום נטענים נכון

## 15. Help / About / PrivacyPolicy

- כל מסך נפתח מתוך התפריט
- אין שאריות אנגלית בשפות שתוקנו
- אין `????`
- אין ג'יבריש בקבצי עברית, רוסית, ערבית, סינית
- קישורים חיצוניים נפתחים

## 16. לוקליזציה

לבדוק לפחות בשפות:
- עברית
- אנגלית
- ערבית
- רוסית
- סינית
- ספרדית
- צרפתית

בכל שפה לבדוק:
- מסך פתיחה
- GameModeSelect
- מסך בחירת משחק אחד לפחות
- מודאל הוראות אחד לפחות
- GameCardModal
- TopMenu
- ShareCard
- About
- Help

## 17. Deep Links

- `loveclient://login` פותח את מסך `Login`
- `loveclient://reset-password?email=...&token=...` פותח את `NewPassword`
- קישור אימות מייל פותח את האפליקציה למסך הנכון
- אם האפליקציה סגורה, ה-deep link עדיין עובד
- אם האפליקציה פתוחה ברקע, ה-deep link עדיין עובד

## 18. בדיקות שרת אחרי פריסה

- `GET /api/Users/test-email` שולח מייל
- רישום יוצר משתמש חדש במסד
- `password-reset/request` מחזיר הצלחה
- `password-reset/confirm` עובד עם token תקין
- שליפת קלפים מחזירה JSON ולא HTML
- אין `500.30`
- אין fallback לדף HTML בתוך האפליקציה

## 19. בדיקות אבטחה ותפעול לפני שחרור

- `appsettings.Secrets.json` לא נכנס ל-git
- `.secrets/service-account.json` לא נכנס ל-git
- `git status` נקי מקבצי סוד
- לא מתבצע `git add -f` על קבצי סוד
- בשרת קיימים קבצי הסוד אחרי כל publish

## 20. הערות שחרור

- אם כפתור `Reset instructions` נועד רק לבדיקה, להסיר לפני פרסום לחנות
- אם `friends_fun` עדיין לא מוכן, לוודא שההודעה שלו ברורה ולא שוברת ניווט
- אחרי החלפת סודות, לבצע smoke test מלא על מייל, DB ו-Firebase

